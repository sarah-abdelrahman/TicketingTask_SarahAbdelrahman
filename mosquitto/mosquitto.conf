# Listen on default MQTT port
listener 1883
protocol mqtt

# Allow local network clients (Docker services)
allow_anonymous true

# Disable persistence for simplicity (optional)
persistence false

# ---- Logging to file ----
log_dest file mosquitto.log
log_type all
log_timestamp true
